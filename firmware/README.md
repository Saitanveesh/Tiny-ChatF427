# Firmware status

The project archive supplied for this public repository contained the Python model/training/evaluation pipeline, tokenizer, checkpoints and results, but it did not contain the final STM32F427 Transformer firmware sources.

Earlier hardware bring-up and tiny-model firmware work exists in the development history, but it is not reconstructed or invented here. This directory is intentionally left as a status marker until the actual firmware source is added.
